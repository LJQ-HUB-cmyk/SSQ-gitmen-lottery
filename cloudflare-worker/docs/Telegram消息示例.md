# Telegram 消息示例

## 每日报告消息格式

### 带策略名称的消息

```
📊 双色球每日报告

━━━━━━━━━━━━━━━
🎰 最新开奖

📅 期号: 2025132
📆 日期: 2025-11-17

🔴 红球: 03 09 14 17 20 27
🔵 蓝球: 12

━━━━━━━━━━━━━━━
🔮 下期预测

组合 1: [频率策略]
🔴 05 12 18 21 28 31
🔵 08

组合 2: [均衡策略]
🔴 02 11 15 23 29 33
🔵 10

组合 3: [冷热号策略]
🔴 07 13 19 24 30 32
🔵 06

━━━━━━━━━━━━━━━
📈 统计信息

高频红球: 03(45), 09(42), 14(40), 17(38), 20(37)
高频蓝球: 12(28), 08(26), 10(24)

━━━━━━━━━━━━━━━
⚠️ 仅供参考，理性购彩
```

## 策略说明

### 显示的策略名称

| 策略 Key | 显示名称 | 说明 |
|---------|---------|------|
| frequency | 频率策略 | 基于历史高频号码 |
| random | 随机策略 | 完全随机选择 |
| balanced | 均衡策略 | 追求号码分布均衡 |
| coldHot | 冷热号策略 | 结合冷热号 |

### 多策略组合示例

当使用多个策略时，每个预测组合会显示对应的策略名称：

```
🔮 下期预测

组合 1: [频率策略]
🔴 03 09 14 17 20 27
🔵 12

组合 2: [频率策略]
🔴 05 12 18 21 28 31
🔵 08

组合 3: [均衡策略]
🔴 02 11 15 23 29 33
🔵 10

组合 4: [均衡策略]
🔴 07 13 19 24 30 32
🔵 06

组合 5: [冷热号策略]
🔴 01 08 16 22 26 33
🔵 14
```

## 配置策略

### Cloudflare Worker 版本

在 `wrangler.toml` 或 `.env` 中配置：

```toml
[vars]
DEFAULT_STRATEGIES = "frequency,balanced,coldHot"
DEFAULT_PREDICTION_COUNT = "9"
```

### Python 版本

```python
from lotteries.ssq.predictor import SSQPredictor

# 使用多个策略
predictor = SSQPredictor(
    history_data, 
    strategies=['frequency', 'balanced', 'coldHot']
)

# 生成预测（每个策略3个）
predictions = predictor.predict(count=9)

# 发送 Telegram 通知
telegram.send_daily_report('ssq', latest_result, predictions, stats)
```

## 消息特点

### 1. 清晰标识
- ✅ 每个预测组合都显示使用的策略
- ✅ 用户可以了解预测的依据
- ✅ 便于评估不同策略的效果

### 2. 格式美观
- ✅ 使用 HTML 格式
- ✅ 策略名称用斜体显示
- ✅ 号码用等宽字体显示

### 3. 信息完整
- ✅ 最新开奖结果
- ✅ 预测组合（含策略）
- ✅ 统计信息
- ✅ 免责声明

## 注意事项

1. **策略名称显示**
   - 只有当预测结果包含 `strategy_name` 字段时才显示
   - 如果没有策略名称，只显示组合编号

2. **兼容性**
   - 向后兼容旧版本预测结果
   - 旧版本预测不会显示策略名称

3. **消息长度**
   - Telegram 消息有长度限制（4096字符）
   - 建议预测组合不超过 5 个

## 测试

### 测试 Telegram 消息

```bash
# Cloudflare Worker 版本
curl -X POST \
  -H "Authorization: Bearer ${API_KEY}" \
  "${WORKER_URL}/run"

# Python 版本
python3 scripts/daily_task.py
```

### 预期效果

消息中应该显示：
- ✅ 策略名称（如 `[频率策略]`）
- ✅ 红球号码
- ✅ 蓝球号码
- ✅ 统计信息

## 示例代码

### JavaScript (Worker)

```javascript
// 预测结果包含策略名称
const predictions = [
  {
    rank: 1,
    red_balls: ['03', '09', '14', '17', '20', '27'],
    blue_ball: '12',
    strategy: 'frequency',
    strategy_name: '频率策略',
    prediction_time: '2025-11-18T10:00:00.000Z'
  }
];

// 发送 Telegram 消息
await telegram.sendDailyReport(latestData, predictions, stats);
```

### Python

```python
# 预测结果包含策略名称
predictions = [
    {
        'rank': 1,
        'red_balls': [3, 9, 14, 17, 20, 27],
        'blue_ball': 12,
        'strategy': 'frequency',
        'strategy_name': '频率策略',
        'prediction_time': '2025-11-18T10:00:00.000Z'
    }
]

# 发送 Telegram 消息
telegram.send_daily_report('ssq', latest_result, predictions, stats)
```
