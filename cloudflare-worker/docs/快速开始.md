# 快速开始指南

## 🚀 四步完成初始化

### 步骤 1：配置默认预测策略和条数（可选）

```bash
cd cloudflare-worker

# 使用单个策略，默认5条
./scripts/set-strategies.sh frequency

# 使用单个策略，指定10条
./scripts/set-strategies.sh frequency 10

# 使用多个策略组合，15条（每个策略5条）
./scripts/set-strategies.sh frequency,balanced,coldHot 15
```

可用策略：
- `frequency` - 频率策略（基于历史高频号码）
- `random` - 随机策略（完全随机）
- `balanced` - 均衡策略（追求号码分布均衡）
- `coldHot` - 冷热号策略（结合冷热号）

建议条数：
- 1个策略：任意条数
- 2个策略：偶数（如 10, 20）
- 3个策略：3的倍数（如 9, 15, 30）
- 4个策略：4的倍数（如 12, 20, 40）

### 步骤 2：部署更新

```bash
npx wrangler deploy
```

### 步骤 3：清空数据库（可选）

在 Cloudflare Dashboard 的 D1 Console 中执行：
```sql
DELETE FROM ssq_lottery;
```

### 步骤 4：运行初始化

```bash
bash cloudflare-worker/init.sh
```

就这么简单！脚本会自动：
- ✅ 每次爬取 100 期
- ✅ 自动去重（存在的跳过）
- ✅ 显示进度
- ✅ 持续运行直到完成

## 📊 查看进度

在另一个终端窗口：

```bash
# 设置代理
export http_proxy="http://127.0.0.1:7897"
export https_proxy="http://127.0.0.1:7897"

# 查看当前数据量
curl -s "https://lottery-prediction.githubmen.workers.dev/stats" | jq '.total_count'
```

## ⏱️ 预计时间

- **每批次**：100 期，约 1-2 分钟
- **达到 1000 期**：约 10 批次，20-30 分钟
- **达到 4000 期**：约 40 批次，80-120 分钟

## 🎯 何时停止

观察脚本输出：

**继续执行**：
```
✅ 本批次成功
   新增: 95 条
   跳过: 5 条
   总计: 200 条
```

**可以停止**：
```
⚠️  本批次大部分数据已存在（跳过 95 条）
可能已经爬取完成
```

或者检查总数：
```bash
curl -s "https://lottery-prediction.githubmen.workers.dev/stats" | jq '.total_count'
```
- 如果 >= 1000，基本完整 ✅
- 如果 >= 4000，完全完整 ✅✅

## 🔄 日常更新

初始化完成后，配置定时任务：

1. 进入 Cloudflare Dashboard
2. Workers & Pages > 你的 Worker > Triggers
3. Add Cron Trigger
4. 设置：`0 14 * * *`（每天 22:00 北京时间）

或手动触发：
```bash
export http_proxy="http://127.0.0.1:7897"
export https_proxy="http://127.0.0.1:7897"

curl -X POST "https://lottery-prediction.githubmen.workers.dev/run" \
  -H "Authorization: Bearer d9464dbad6564438a37ff5245494152d"
```

## 🛠️ 故障排查

### 问题 1：连接超时

确保代理已设置：
```bash
export http_proxy="http://127.0.0.1:7897"
export https_proxy="http://127.0.0.1:7897"
```

### 问题 2：认证失败

检查 API_KEY 是否正确：
```bash
# 在 init.sh 中查看
grep API_KEY cloudflare-worker/init.sh
```

### 问题 3：数据不增加

查看 Worker 日志：
- Cloudflare Dashboard > Workers > 你的 Worker > Logs

## 📱 Telegram 通知

如果配置了 Telegram，会收到通知：

```
✅ 批量导入完成

新增: 95 条
跳过: 5 条
当前总计: 200 条

💡 继续触发 /init 可导入更多历史数据
```

## 🎉 完成后

初始化完成后，你可以：

1. **查看最新数据**：
   ```bash
   curl -s "https://lottery-prediction.githubmen.workers.dev/latest"
   ```

2. **查看统计**：
   ```bash
   curl -s "https://lottery-prediction.githubmen.workers.dev/stats"
   ```

3. **获取预测**：
   ```bash
   curl -s "https://lottery-prediction.githubmen.workers.dev/predict"
   ```

---

**就是这么简单！** 🚀
